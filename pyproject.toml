[build-system]
requires = ["setuptools>=80", "setuptools-git-versioning>=2.1"]
build-backend = "setuptools.build_meta"

[project]
name = "camillo"
dynamic = ["version"]
description = "A Postgres-native single-user cognitive memory stack with shared inference."
requires-python = ">=3.14"
dependencies = [
    "fastapi>=0.115.0",
    "uvicorn[standard]>=0.30.0",
    "sqlalchemy[asyncio]>=2.0.0",
    "asyncpg>=0.29.0",
    "alembic>=1.13.0",
    "pydantic>=2.8.0",
    "pydantic-settings>=2.4.0",
    "pgvector>=0.3.0",
    "shared-inference",
    "mcp>=1.0.0",
    "jinja2>=3.1.0",
    "loguru>=0.7.0",
    "python-dotenv>=1.0.0",
]

[project.optional-dependencies]
trace = [
    "arize-phoenix-otel>=0.15.0",
]
dev = [
    "pytest>=8.0.0",
    "pytest-asyncio>=0.23.0",
    "ruff>=0.6.0",
    "scalene>=1.5.50",
]

[tool.setuptools.packages.find]
where = ["src"]

[tool.uv.sources]
shared-inference = { git = "https://github.com/Enucatl/shared-inference.git", branch = "main" }

[tool.setuptools.package-data]
camillo = ["ai/templates/*.jinja"]

[tool.setuptools-git-versioning]
enabled = true

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
markers = [
    "integration: requires a running PostgreSQL/pgvector database",
    "performance: synthetic performance checks, opt-in by environment",
]

[tool.ruff]
line-length = 100
target-version = "py314"

[tool.ruff.lint]
select = ["ASYNC", "B", "E", "F", "I", "RUF", "UP"]
ignore = ["B008"]
